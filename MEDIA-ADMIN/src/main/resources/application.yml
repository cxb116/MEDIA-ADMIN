server:
  port: 8888

spring:
  application:
    name: media-admin

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://115.191.52.226:3306/dsp-admin?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&useLocalSessionState=true&rewriteBatchedStatements=true
    username: root
    password: root123456
    # HikariCP 连接池配置
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 540000  # 9分钟，小于MySQL默认的wait_timeout(8小时)，避免连接被关闭
      connection-test-query: SELECT 1
      validation-timeout: 5000
      leak-detection-threshold: 60000  # 检测连接泄漏
      pool-name: MediaAdminHikariCP

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
    database-platform: org.hibernate.dialect.MySQL8Dialect

# JWT配置
jwt:
  secret: media-admin-secret-key-for-jwt-token-generation-2024-secure-key-hs512-must-be-at-least-64-bytes-long
  expiration: 86400000 # 24小时（毫秒）

logging:
  level:
    com.media.admin: debug
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace
